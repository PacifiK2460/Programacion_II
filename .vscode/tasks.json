{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug",
            "type": "shell",
            "command": "cd debug && cmake -DCMAKE_BUILD_TYPE=Debug -G \"MinGW Makefiles\" .. && cmake --build ."
        },{
            "label": "Build",
            "type": "shell",
            "command": "cd build && cmake .. -G \"MinGW Makefiles\" && cmake --build ."
        }
    ]
}